meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/deleteUser?userId={{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

params:query {
  userId:
}

meta {
  name: Update Terminal
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/updateTerminal?terminalId={{terminalId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

params:query {
  terminalId:
}

body:json {
  {
    "name": ""
  }
}

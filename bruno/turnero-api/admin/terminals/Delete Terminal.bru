meta {
  name: Delete Terminal
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/deleteTerminal?terminalId={{terminalId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

params:query {
  terminalId:
}

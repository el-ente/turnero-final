meta {
  name: Create Queue
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/createQueue
  body: json
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

body:json {
  {
    "name": "",
    "sectorId": ""
  }
}

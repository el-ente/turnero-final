meta {
  name: Delete Queue
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/deleteQueue?queueId={{queueId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

params:query {
  queueId:
}

meta {
  name: Delete Sector
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/deleteSector?sectorId={{sectorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

params:query {
  sectorId:
}
